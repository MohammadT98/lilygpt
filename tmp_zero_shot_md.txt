---MARKDOWN CELL 0---
# Zero-shot Generation - End-to-End Evaluation Pipeline

This notebook implements the **evaluation side** of the Zero-shot Generation.

### Notebook Structure
Given a directory of model generations, it runs a full pipeline:

1. **LilyPond → MIDI rendering**  
   - Wraps raw LilyPond snippets into a valid `\score` block if needed.  
   - Calls the system LilyPond binary, captures logs, and writes a cleaned `.ly` with `\midi` and `\layout` blocks.

2. **MIDI-level musical analysis (music21)**  
   - Parses the rendered MIDI and computes structural and musical descriptors: note count, range, interval entropy, step vs. leap, bar structure, key adherence, tonal stability, contour stats, etc.

3. **LilyPond text-level evaluation**  
   - Inspects the LilyPond source directly for adherence to our prompt constraints: relative/absolute notation, lowercase, forbidden constructs, bar structure, final barline, etc.  

4. **Batch aggregation & reporting**  
   - For each batch, writes JSONL logs per stage, a combined `final/index.jsonl`, and an `AVG_row.json` summary.  
   - Builds a human-readable Pandas table per batch, with an extra **AVG** row summarizing compile rates, in-key percentage, style label distribution (scalar vs balanced), and other metrics.

### Requirements
- **Python 3.10+**
- **LilyPond 2.24.4** installed and reachable from:
  - PATH, or  
  - `LILYPOND_BIN` environment variable, or  
  - one of the default Windows locations.
- **Python libraries:**
  - `music21`
  - `pandas`
  - `IPython`

### Output
The goal is to treat this notebook as the **single entry point** for evaluating prompt configurations: once generations are saved under `zero_shot_outputs/raw/`, we can run this file to obtain both per-sample diagnostics and batch-level statistics ready for analysis and thesis figures.

---MARKDOWN CELL 1---
## Block 1 — LilyPond → MIDI renderer (`lily_to_midi`)

This block is responsible for taking a **raw LilyPond snippet** (as generated by the model) and turning it into a **MIDI file** in a robust way.

Main responsibilities:

- **LilyPond binary discovery**
  - `find_lilypond()` searches for a usable LilyPond executable via PATH, `LILYPOND_BIN`, or a Windows default path.
  - `_is_working_lilypond()` sanity-checks the executable by calling `--version`.

- **Score wrapping / sanitization**
  - `_extract_version_line()` pulls out an existing `\version` line so it can be preserved.
  - `_pull_block_by_name()` extracts and reuses `\header` and `\paper` blocks if they exist.
  - `_hoist_top_level_assignments()` moves top-level variable assignments (like `mySeq = { ... }`) to the top of the file so they are visible inside the score.
  - `_has_top_level_staff()` decides whether to wrap the music in a `\new Staff` or reuse existing staff contexts.
  - `_build_score_from_body()` assembles a complete `\score { ... }` block, and ensures that `\layout {}` and `\midi {}` sub-blocks are present.

- **Rendering to MIDI**
  - `lily_to_midi()`:
    - Derives an output MIDI filename from the input `.ly`.
    - Caches and reuses an existing MIDI file if `force=False` and the target already exists.
    - Writes a cleaned/augmented `.ly` (`ly_with_midi_*.ly`).
    - Calls LilyPond in a temporary directory, then moves the produced MIDI into `midi_dir`.
    - Writes a detailed `.log` file on failure, including command line and LilyPond stdout/stderr.

- **Return structure**
  - Returns a structured dict with:
    - `ok` flag and `reason` (`rendered`, `exists`, `encoding_error`, `lilypond_failed`, `no_midi_output`).
    - Timing information (`seconds`).
    - Paths to the MIDI and `ly_with_midi` files.
    - SHA-256 hash of the original `.ly` source.
    - Truncated error message for logging.

This block is purely about **getting a MIDI file out of whatever LilyPond the model produced**, while being as tolerant as possible to partial or messy input.

---MARKDOWN CELL 3---
## Block 2 — MIDI-level musical analysis (`eval_midi`)

Once we have a MIDI file, this block uses **music21** to compute a rich set of musical descriptors.  
It treats the MIDI as the “ground truth performance” of the generated piece, independent of how the LilyPond text looks.

Main pieces:

- **Core analysis**
  - `_analyze_midi_with_music21()` loads the MIDI into music21, flattens the score, and separates:
    - Notes
    - Chords
    - Measures
  - It builds a time-ordered “melodic line” (notes + chord basses) and derives:
    - `intervals` between successive pitches
    - global pitch range (`min_midi`, `max_midi`)

- **Melodic / structural metrics**
  - `interval_entropy`: Shannon entropy of the interval distribution.
  - `step_vs_leap`: proportion of small steps (≤ 2 semitones) vs larger leaps.
  - `direction_changes`: number of times the melodic contour changes up ↔ down.
  - `repeat_rate`: proportion of repeated notes.
  - `avg_interval_size`: mean absolute interval size.
  - `note_density`: notes per bar (or per piece if bar info is missing).
  - `rhythmic_diversity`: count of distinct rhythmic values (quarterLength variants).

- **Bar / time-signature metrics**
  - `_compute_beats_per_bar_match_rate()` compares the sum of durations in each measure against the expected total from the declared time signature.
  - `_compute_expected_bars()` infers how many bars the piece should have and compares it to the observed bar count.
  - Derived booleans:
    - `beats_ok`: beats per bar mostly consistent with time signature.
    - `bars_ok_musical`: bar count consistent (within ±1) with expected length.

- **Key / tonal metrics**
  - `_compute_detected_key()` runs music21’s key analysis and normalizes the key string.
  - `_compute_in_key_share_and_cadence()`:
    - Measures the time spent in the declared key (time-weighted in-key percentage).
    - Checks whether the piece ends on the tonic.
    - Returns `in_key_pct_time_weighted`, `ends_on_tonic`, and a boolean `in_key_ok` based on a threshold.
  - `_compute_tonal_stability()`:
    - Splits the piece into segments.
    - Runs local key analysis per segment.
    - Reports a `tonal_stability_score` (how often segments agree), `key_changes`, and `drift_detected`.

- **Contour metrics**
  - `_compute_contour_analysis()` reduces the pitch line to up/down/same symbols and counts:
    - Dominant contour patterns,
    - `contour_diversity`,
    - `ascending_tendency` / `descending_tendency`,
    - `arch_shapes` and `downward_arches`.

- **Public API**
  - `eval_midi(midi_path, declared_key_pc_mode, declared_time, ...)`:
    - Wraps the call with timing and error handling.
    - Returns:
      - `ok` flag,
      - `seconds` (runtime),
      - `metrics` dict (all numbers above),
      - `error` string and `tooling` (music21 version).

This block gives us a **high-level musical profile** of each generation, which we later aggregate per batch (compile rates, scalar vs balanced style, stability, etc.).

---MARKDOWN CELL 5---
## Block 3 — LilyPond text-level evaluation (`eval_lily`)

This block inspects the **LilyPond source text itself** to see how well the model followed the prompt constraints, even before (or in addition to) actual rendering.

Key responsibilities:

- **Declared key/time & notation detection**
  - `_extract_declared()` parses the `.ly` file for:
    - Declared key as `(tonic_pc, mode)`
    - Declared time signature `(num, den)`
    - Notation type: `"relative"` vs `"absolute"` (based on `\relative` usage)

- **Body extraction & tokenization**
  - `strip_comments()` removes `%` line comments and `%{ ... %}` block comments.
  - `extract_music_body()` tries to isolate the main music block:
    - Prefer a `\relative c' { ... }` block.
    - Otherwise take the first `{ ... }` block or fall back to whole cleaned text.
  - `tokenize_notes_and_bars()` turns the body into a flat sequence:
    - `("note", name, accidental, marks, duration, dots)`
    - `("bar",)` and `("bar_cmd", ...)`
  - `bars_from_tokens()` groups tokens into literal bars.

- **Prompt adherence checks**
  - Notation policy:
    - If `expected_notation="relative"`, require at least one `\relative c' { ... }` anchor.
    - If `expected_notation="absolute"`, require no `\relative`.
  - Case policy:
    - When `require_lowercase=True`, disallow uppercase note names (C, D, E, …).
  - Forbidden constructs:
    - Rests (`r`), chords (`<...>`), multi-voice constructs (`<< >> \\`), repeats, tuplets, ties, grace notes, skips, `\score` and `\layout` blocks inside the body, etc.
  - Structural checks:
    - `bar_count_literal`: number of bars inferred from `|` / `\bar`.
    - `final_barline_literal_ok`: last token is a bar/bar_cmd (optional).
    - `bars_literal_ok`: bar count close (±1) to `expected_bars_literal`.
    - `notes_per_bar_literal_ok`: bar durations close to `expected_notes_per_bar_literal` in quarter-note units.
  - Accidentals policy:
    - Optionally disallow accidentals if `allow_accidentals=False`.

- **Compile / parse validation**
  - If `lilypond_bin` is provided:
    - Write a temp `.ly` (inserting `\version` if missing).
    - Invoke LilyPond with a “null” backend to just check for syntax.
    - Capture return code and a truncated error message.
  - Otherwise, or if LilyPond is not available:
    - Try a music21-based LilyPond parse as a lighter sanity check.
  - Fields:
    - `compiles` (True/False/None),
    - `compile_error`,
    - `compile_via` (`"lilypond"` / `"music21"` / None),
    - `parser_error` (for music21 failures).

- **Adherence score**
  - Collect all boolean flags (key/time present, notation_ok, lowercase_ok, no_forbidden, etc.).
  - Compute a normalized `adherence_text` in `[0,1]` plus a list of `failed_flags`.

- **Public entry**
  - `eval_lily(out_ly, ...)`:
    - Loads a `.ly` file from disk.
    - Calls `evaluate_lilypond_text()` with our configuration.
    - Wraps the result with timing and `tooling` info (`lilypond_bin` path).

This block tells us **how “clean” and on-spec the LilyPond text is**, independently of whether the MIDI rendering and musical content are good.

---MARKDOWN CELL 7---
## Block 4 — Integrated pipeline runner & batch summaries

This final block stitches everything together into a **single end-to-end pipeline** operating on the `zero_shot_outputs` directory.

Main responsibilities:

- **Paths & environment**
  - `_nb_base_dir()` and `OUTPUTS_ROOT` define where to look for:
    - `raw/` → model generations (`out_*.ly`)
    - `midi/` → rendered MIDI and logs
    - `eval_midi/` → MIDI evaluation JSONL
    - `eval_lily/` → text evaluation JSONL
    - `final/` → combined per-run + per-batch summaries
  - `find_lilypond_bin()` tries to auto-detect a LilyPond binary and reports it in a small Markdown banner.

- **Batch discovery & bookkeeping**
  - `_collect_batches()` groups `out_*.ly` files by batch subdirectory.
  - `list_batches()` lists available batch names.
  - `_load_generation_map()` loads `summary.json` from generation-time and maps `run` indices to generation metadata (tokens, elapsed seconds, model, etc.).

- **Empty-output detection**
  - `_is_effectively_empty_ly()` and helpers detect “empty” generations:
    - Only whitespace/comments.
    - No notes, blocks, or key/time directives.
  - These are flagged upfront as `status="empty"` and skip heavy stages.

- **Status logic**
  - `_status_from(ok_midi_render, ok_midi_eval, ok_lily_eval, is_empty, compiles)` collapses all stage outcomes into a single label:
    - `"empty"`
    - `"failed_to_compile"` (LilyPond text doesn’t compile/parse)
    - `"ok"` (render, MIDI eval, and Lily eval all ok)
    - `"lily_only_ok"` (Lily eval ok but MIDI render/eval failed)
    - `"failed"` (other combinations)

- **Per-file pipeline**
  For each `out_*.ly` in a batch:
  1. Detect `is_empty` and try to extract declared key/time/notation with `_extract_declared()`.
  2. Run **Lily → MIDI** via `lily_to_midi()` (unless empty).
  3. Run **MIDI evaluation** via `eval_midi()` if MIDI exists.
  4. Run **Lily text evaluation** via `eval_lily()` (unless empty).
  5. Consolidate everything into a single `final_rec` with:
     - generation metadata,
     - render + MIDI metrics,
     - Lily metrics,
     - `status` and a `style_label` (`"scalar"` vs `"balanced"` based on step/entropy heuristics).
  6. Append that record to `final/index.jsonl`.

- **Human-readable tables**
  - Build a per-batch DataFrame with key columns (compile status, musical metrics, adherence, style label, note counts, etc.).
  - Convert booleans into emojis (✅/❌) where appropriate.
  - Round numeric values for display (3 decimals).
  - Special formatting:
    - `in_key_pct_time_weighted` and `adherence_text` are shown as percentages if they look like fractions.

- **AVG row & JSON summary**
  - Compute an extra **AVG row** per batch:
    - Percent “ok”.
    - Compile / in-key / beats / bars / drift rates.
    - Mean in-key percentage, beats-per-bar match rate, tonal stability, contour diversity, ascending tendency.
    - Mean adherence, plus “% scalar” among compiled rows.
    - Average note count and literal bar count over successful runs.
  - Attach this AVG row to the bottom of the displayed table.
  - Serialize it to `final/AVG_row.json` for downstream analysis (plots, thesis tables).

- **Public API & simple UI**
  - `run_full_pipeline_for_all(...)` is the main programmatic entry point; it returns:
    - `dfs_by_batch`: the displayed DataFrames (including AVG row)
    - `avg_rows_by_batch`: plain dict summaries.

This block turns the three previously defined components—renderer, MIDI evaluator, and Lily text evaluator—into a **repeatable, dataset-wide evaluation pipeline** with structured logs and ready-to-use aggregate metrics.

---MARKDOWN CELL 9---
--------------------------
---MARKDOWN CELL 10---
<div style="background-color:#f2f2f2;padding:10px;border-radius:8px;">
  <h3 style="color:black;">🎵 Key</h3>
</div>
---MARKDOWN CELL 11---
## 🧪 C Major
---MARKDOWN CELL 13---
### Observation
The C-major batch was extremely stable: 100% compile, perfect bar/beat structure (100% / 100%), and fully in-key (100% in-key, 100% tonal stability, no drift). Melodically it stayed simple, with interval entropy 2.03, strong stepwise motion (step_share 0.884, avg interval 2.22), and only 5.25 direction changes on average, giving a smooth scalar feel. Density was low (4 notes/bar, 32 notes over 8 bars). Complexity stayed modest (repeat_rate 0.044, contour_diversity 0.168), with a slightly upward bias (ascending_tendency 0.65). Generation cost was normal (1943 tokens, 14s).
---MARKDOWN CELL 14---
-----------------
---MARKDOWN CELL 15---
## 🧪 F Major
---MARKDOWN CELL 17---
### Observation
The F-major batch was also very solid: 100% compile, perfect rhythmic structure (100% / 100%, 1.0 match rate), and fully in key (100% in-key, 100% tonal stability, no drift). Compared to C major, the melodies were a bit more varied: interval entropy 1.46, avg interval 2.91, and 8 direction changes, while still mostly stepwise (step_share 0.861). Density stayed identical (4 notes/bar, 32 notes over 8 bars). Repetition was extremely low (repeat_rate 0.005), contour diversity slightly higher (0.188), and the line leaned upward (ascending_tendency 0.835). Generation cost was similar (2412 tokens, 14.06s).
---MARKDOWN CELL 18---
-----------
---MARKDOWN CELL 19---
## 🧪  E Minor
---MARKDOWN CELL 21---
### Observation
The E-minor batch was noticeably weaker: 90% compile and only 90% bar/beat correctness (0.90 match rate). Tonality also slipped a bit, with 85% in-key and 0.375 tonal stability. The melodic profile stayed similar to the major runs—interval entropy 1.53, avg interval 2.74, and about 8.89 direction changes—mostly stepwise (step_share 0.853). Density stayed fixed (4 notes/bar, 32 notes in 8 bars), and repetition remained very low (repeat_rate 0.007). Contour diversity (0.105) was lower than in F major, and ascending tendency stayed moderate (0.715). Generation cost was slightly higher (2350 tokens, 16.2s).
---MARKDOWN CELL 22---
-----------
---MARKDOWN CELL 23---
## 🧪  D Dorian
---MARKDOWN CELL 25---
### Observation
The D-Dorian batch behaved similarly to E minor: 90% compile, 90% bar/beat correctness, and a 0.90 beat-match rate. Tonality stayed steadier though, with 90% in-key and 0.00 tonal drift. Melodically it kept the same safe profile: interval entropy 1.52, avg interval 2.81, and 8 direction changes, with strong stepwise preference (step_share 0.858). Density stayed fixed (4 notes/bar, 32 notes across 8 bars), and repetition was almost zero (repeat_rate 0.002). Contour diversity was low (0.098) and the ascending tendency was moderate (0.738). Generation cost was slightly higher (2451 tokens, 18.0s).
---MARKDOWN CELL 26---
-----------
---MARKDOWN CELL 27---
## 🧪 G Mixolydian
---MARKDOWN CELL 29---
### Observation
The G-Mixolydian batch was the weakest so far: only 85% compile, 85% bar/beat correctness, and a 0.85 beat-match rate. Tonality also held at 85% in-key with zero drift. Melodically it stayed simple but with slightly bigger motion: interval entropy 1.31, avg interval 3.06, about 8.24 direction changes, and a strong stepwise bias (step_share 0.854). Density stayed fixed (4 notes/bar, 32 notes in 8 bars), and repetition remained minimal (repeat_rate 0.006). Contour diversity was low (0.087) with a moderate upward lean (ascending_tendency 0.725). Generation cost was normal (2406 tokens, 15.87s).
---MARKDOWN CELL 30---
-----------
---MARKDOWN CELL 31---
## 🧪 G Major (1♯)
---MARKDOWN CELL 33---
### Observation
The G-major batch landed in the mid range: 90% compile, 90% bar/beat correctness, and a 0.90 beat-match rate. Tonality was a bit weaker than the other major keys, with 75% in-key and 0.075 tonal stability. Melodically it moved a bit more: interval entropy 1.54, avg interval 3.37, and about 9.44 direction changes, while still mostly stepwise (step_share 0.815). Density stayed fixed (4 notes/bar, 32 notes over 8 bars), and repetition remained low (repeat_rate 0.007). Contour diversity was mild (0.102) with a stable upward lean (ascending_tendency 0.719). Generation cost was on the lighter side (2241 tokens, 13.44s).
---MARKDOWN CELL 34---
----------------
---MARKDOWN CELL 35---
## 🧪 D Major (2♯)
---MARKDOWN CELL 37---
### Observation
The D-major batch was structurally perfect: 100% compile, 100% bar/beat correctness, and a 1.00 match rate. Tonality held reasonably well with 80% in-key and 0.062 tonal stability. The melodic profile was a bit more active than the other majors: interval entropy 1.59, avg interval 3.35, and about 9.4 direction changes, while still leaning stepwise (step_share 0.819). Density stayed fixed (4 notes/bar, 32 notes across 8 bars), and repetition stayed low (repeat_rate 0.008). Contour diversity was modest (0.117) with a stronger upward tendency (0.801). Generation cost was light (2166 tokens, 13.8s).
---MARKDOWN CELL 38---
----------------
---MARKDOWN CELL 39---
## 🧪 B♭ Major (2♭)
---MARKDOWN CELL 41---
### Observation
The B-major batch was mixed: 95% compile, 95% bar/beat correctness, and a 0.95 match rate. Tonality dropped compared to other majors, with 70% in-key and 0.150 tonal stability. Melodically it was more varied: interval entropy 1.93, avg interval 2.47, and about 6.10 direction changes, still mostly stepwise (step_share 0.881). Density was slightly lower (3.958 notes/bar, 32 notes over 8 bars). Repetition stayed minimal (repeat_rate 0.014), contour diversity remained modest (0.135), and the ascending tendency was mild (0.629). Generation cost was normal (2370 tokens, 15.9s).
---MARKDOWN CELL 42---
----------------
---MARKDOWN CELL 43---
## 🧪 A Minor (0)
---MARKDOWN CELL 45---
### Observation
The A-minor batch was very stable: 100% compile, 100% bar/beat correctness, and a 1.00 match rate. Tonality was perfect (100% in-key) with a fairly strong stability score (0.487). Melodically it moved more than most keys: interval entropy 1.85, avg interval 3.17, and 10 direction changes, while still mostly stepwise (step_share 0.802). Density stayed standard (4 notes/bar, 32 notes in 8 bars), and repetition stayed tiny (repeat_rate 0.005). Contour was modest (0.117) with a moderate upward lean (ascending_tendency 0.659). Generation cost was normal (2250 tokens, 14.68s).
---MARKDOWN CELL 46---
----------------
---MARKDOWN CELL 47---
## 🧪 D Minor (1♭)
---MARKDOWN CELL 49---
### Observation
The D-minor batch held up well overall: 95% compile, 95% bar/beat correctness, and a 0.95 match rate. Tonality was strong with 95% in-key and a high 0.475 stability score. The melodic profile stayed moderate: interval entropy 1.68, avg interval 2.81, and about 8.68 direction changes, mostly stepwise (step_share 0.857). Density was standard (4 notes/bar, 32 notes across 8 bars), and repetition stayed very low (repeat_rate 0.007). Contour diversity was modest (0.117) with a stable upward tendency (0.716). Generation cost was typical (2370 tokens, 15.40s).
---MARKDOWN CELL 50---
----------------
---MARKDOWN CELL 51---
## 🧪 C Lydian (modal, 1♯)
---MARKDOWN CELL 53---
### Observation
The C-Lydian batch was decent: 90% compile, 90% bar/beat correctness, and a 0.90 match rate. Tonality held steady with 90% in-key and no drift. Melodically it stayed fairly simple: interval entropy 1.71, avg interval 2.43, and about 6.11 direction changes, with a strong stepwise bias (step_share 0.910). Density was standard (4 notes/bar, 32 notes in 8 bars), and repetition was low (repeat_rate 0.016). Contour diversity remained modest (0.123) and the ascending tendency was mild (0.619). Generation cost was light (2330 tokens, 13.48s).
---MARKDOWN CELL 54---
----------------
---MARKDOWN CELL 55---
<div style="background-color:#f2f2f2;padding:10px;border-radius:8px;">
  <h3 style="color:black;">🎵 Tempo</h3>
</div>
---MARKDOWN CELL 56---
## 🧪  Slow Tempo
---MARKDOWN CELL 58---
### Observation
The slow-tempo batch was very clean: 100% compile, 100% bar/beat correctness, and a 1.00 match rate. Tonally it stayed perfect (100% in-key) with a moderate stability score (0.275). Melodically it kept things simple: interval entropy 1.95, avg interval 2.17, and about 5.65 direction changes, mostly stepwise (step_share 0.881). Density was standard (4 notes/bar, 32 notes over 8 bars), with moderate repetition (repeat_rate 0.045). Contour diversity was low (0.158) and ascending tendency sat around 0.656. Generation cost was the lightest so far (1830 tokens, 10.64s).
---MARKDOWN CELL 59---
-----------
---MARKDOWN CELL 60---
## 🧪  Fast Tempo
---MARKDOWN CELL 62---
### Observation
The fast-tempo batch was flawless structurally: 100% compile, 100% bar/beat correctness, and a 1.00 match rate. Tonality held perfectly (100% in-key) with a modest stability score (0.113). Melodically it stayed very controlled: interval entropy 1.76, avg interval 2.26, and 5 direction changes, with strong stepwise motion (step_share 0.911). Density remained standard (4 notes/bar, 32 notes in 8 bars), and repetition was low (repeat_rate 0.034). Contour diversity was low (0.148) and ascending tendency sat at 0.716. Generation cost was the lowest (1652 tokens, 9.23s).
---MARKDOWN CELL 63---
-----------
---MARKDOWN CELL 64---
<div style="background-color:#f2f2f2;padding:10px;border-radius:8px;">
  <h3 style="color:black;">🎵 Time Signature</h3>
</div>
---MARKDOWN CELL 65---
## 🧪 3/4 (simple triple)
---MARKDOWN CELL 67---
### Observation
The dyads batch stayed perfectly stable: 100% compile, 100% bar/beat correctness, and a 1.00 match rate. Tonality was flawless (100% in-key) with a moderate stability score (0.212). Despite using dyads, the melodic stats mirror simple textures: interval entropy 1.45, avg interval 2.68, and 5.95 direction changes, with strong stepwise motion (step_share 0.863). Density matched the setup (3 notes/bar, 24 notes across 8 bars). Repetition was minimal (repeat_rate 0.013), contour diversity stayed low (0.159), and ascending tendency was high (0.786). Generation cost was standard (1979 tokens, 12.31s).
---MARKDOWN CELL 68---
## 🧪 6/8 (compound duple)
---MARKDOWN CELL 70---
### Observation
The 6/8 batch was completely stable: 100% compile, 100% bar/beat correctness, and a 1.00 match rate. Tonality stayed perfect (100% in-key) with a moderate stability score (0.250). Melodically it moved more than the simple meters: interval entropy 1.54, avg interval 2.63, and a high 11.7 direction changes, though still mostly stepwise (step_share 0.871). Density matched the meter (6 notes/bar, 48 notes across 8 bars), and repetition was low (repeat_rate 0.018). Contour diversity was modest (0.084) with a strong upward lean (ascending_tendency 0.774). Generation cost was typical (2103 tokens, 13.42s).
---MARKDOWN CELL 71---
-----------
---MARKDOWN CELL 72---
<div style="background-color:#f2f2f2;padding:10px;border-radius:8px;">
  <h3 style="color:black;">🎵 Harmony </h3>
</div>
---MARKDOWN CELL 73---
## 🧪 Dyads (2-note chords)
---MARKDOWN CELL 75---
### Observation
The dyads batch was moderately stable: 95% compile, 95% bar/beat correctness, and a 0.95 match rate. Tonality held well (95% in-key, stability 0.250). The texture was much denser than the single-note runs: 8 notes/bar (64 notes across 8 bars) with very low repetition (0.002) and a high 13.42 direction changes. Melodically it stayed moderate—interval entropy 1.72, avg interval 2.56, and mostly stepwise motion (step_share 0.801). Contour diversity was low (0.108) with a mild upward lean (0.677). Generation cost jumped due to chordal density (3391 tokens, 21.91s).
---MARKDOWN CELL 76---
## 🧪 Triads (3-note chords)
---MARKDOWN CELL 78---
### Observation
The triads batch struggled: only 80% compile, 80% bar/beat correctness, and a 0.80 match rate. Tonality also dropped to 80% in-key with a relatively high stability score (0.562) but clear drift issues (50% drift-detected). The texture was very dense: 11.953 notes/bar (96 notes across 8 bars). Melodically it got messier—interval entropy 2.00, avg interval 2.25, and 12.75 direction changes, though still mostly stepwise (step_share 0.801). Repetition increased sharply (0.098), contour diversity stayed moderate (0.141), and ascending tendency weakened (0.494). Generation cost was the highest so far (3489 tokens, 25.64s).
---MARKDOWN CELL 79---
-----------
---MARKDOWN CELL 80---
## Conclusion
Across all zero-shot tests, the model handled the basics reliably: clean bar structures, solid rhythm, and generally strong tonal adherence in simple textures. Major and minor keys stayed stable, with compile rates between 85–100%, while modal keys and more complex setups (like Mixolydian or B major) showed small drops in in-key accuracy and stability. Meter changes behaved well, with 3/4 and 6/8 adapting cleanly and keeping density exactly where expected. The real stress test was harmony: dyads increased complexity and raised token cost, and triads pushed the model near its limit—compile rate fell to 80%, drift jumped, density exploded, and melodic control weakened. Overall, the model is highly reliable for single-line melodic generation and light harmony, but it starts breaking once the harmonic texture gets too dense. This gives us a clear baseline before moving into few-shot and fine-tuning.
