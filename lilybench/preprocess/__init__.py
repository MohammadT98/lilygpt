"""Preprocessing: dataset building, augmentation, and work-based splitting.

Submodules are imported explicitly by consumers so this package can be used
without the training extras (``torch``, ``peft``) installed.
"""
