---MARKDOWN CELL 0---
# Prompt Lab - End-to-End Evaluation Pipeline

This notebook implements the **evaluation side** of the Prompt Lab.

The purpose of this notebook is to guide our decisions on which normalization rules should be applied, which ones help the model, and which ones introduce noise before we finalize the dataset for fine-tuning.

### Notebook Structure
Given a directory of model generations, it runs a full pipeline:

1. **LilyPond → MIDI rendering**  
   - Wraps raw LilyPond snippets into a valid `\score` block if needed.  
   - Calls the system LilyPond binary, captures logs, and writes a cleaned `.ly` with `\midi` and `\layout` blocks.

2. **MIDI-level musical analysis (music21)**  
   - Parses the rendered MIDI and computes structural and musical descriptors: note count, range, interval entropy, step vs. leap, bar structure, key adherence, tonal stability, contour stats, etc.

3. **LilyPond text-level evaluation**  
   - Inspects the LilyPond source directly for adherence to our prompt constraints: relative/absolute notation, lowercase, forbidden constructs, bar structure, final barline, etc.  

4. **Batch aggregation & reporting**  
   - For each batch, writes JSONL logs per stage, a combined `final/index.jsonl`, and an `AVG_row.json` summary.  
   - Builds a human-readable Pandas table per batch, with an extra **AVG** row summarizing compile rates, in-key percentage, style label distribution (scalar vs balanced), and other metrics.

### Requirements
- **Python 3.10+**
- **LilyPond 2.24.4** installed and reachable from:
  - PATH, or  
  - `LILYPOND_BIN` environment variable, or  
  - one of the default Windows locations.
- **Python libraries:**
  - `music21`
  - `pandas`
  - `IPython`

### Output
The goal is to treat this notebook as the **single entry point** for evaluating prompt configurations: once generations are saved under `prompt_lab_outputs/raw/`, we can run this file to obtain both per-sample diagnostics and batch-level statistics ready for analysis and thesis figures.

---MARKDOWN CELL 1---
## Block 1 — LilyPond → MIDI renderer (`lily_to_midi`)

This block is responsible for taking a **raw LilyPond snippet** (as generated by the model) and turning it into a **MIDI file** in a robust way.

Main responsibilities:

- **LilyPond binary discovery**
  - `find_lilypond()` searches for a usable LilyPond executable via PATH, `LILYPOND_BIN`, or a Windows default path.
  - `_is_working_lilypond()` sanity-checks the executable by calling `--version`.

- **Score wrapping / sanitization**
  - `_extract_version_line()` pulls out an existing `\version` line so it can be preserved.
  - `_pull_block_by_name()` extracts and reuses `\header` and `\paper` blocks if they exist.
  - `_hoist_top_level_assignments()` moves top-level variable assignments (like `mySeq = { ... }`) to the top of the file so they are visible inside the score.
  - `_has_top_level_staff()` decides whether to wrap the music in a `\new Staff` or reuse existing staff contexts.
  - `_build_score_from_body()` assembles a complete `\score { ... }` block, and ensures that `\layout {}` and `\midi {}` sub-blocks are present.

- **Rendering to MIDI**
  - `lily_to_midi()`:
    - Derives an output MIDI filename from the input `.ly`.
    - Caches and reuses an existing MIDI file if `force=False` and the target already exists.
    - Writes a cleaned/augmented `.ly` (`ly_with_midi_*.ly`).
    - Calls LilyPond in a temporary directory, then moves the produced MIDI into `midi_dir`.
    - Writes a detailed `.log` file on failure, including command line and LilyPond stdout/stderr.

- **Return structure**
  - Returns a structured dict with:
    - `ok` flag and `reason` (`rendered`, `exists`, `encoding_error`, `lilypond_failed`, `no_midi_output`).
    - Timing information (`seconds`).
    - Paths to the MIDI and `ly_with_midi` files.
    - SHA-256 hash of the original `.ly` source.
    - Truncated error message for logging.

This block is purely about **getting a MIDI file out of whatever LilyPond the model produced**, while being as tolerant as possible to partial or messy input.

---MARKDOWN CELL 3---
## Block 2 — MIDI-level musical analysis (`eval_midi`)

Once we have a MIDI file, this block uses **music21** to compute a rich set of musical descriptors.  
It treats the MIDI as the “ground truth performance” of the generated piece, independent of how the LilyPond text looks.

Main pieces:

- **Core analysis**
  - `_analyze_midi_with_music21()` loads the MIDI into music21, flattens the score, and separates:
    - Notes
    - Chords
    - Measures
  - It builds a time-ordered “melodic line” (notes + chord basses) and derives:
    - `intervals` between successive pitches
    - global pitch range (`min_midi`, `max_midi`)

- **Melodic / structural metrics**
  - `interval_entropy`: Shannon entropy of the interval distribution.
  - `step_vs_leap`: proportion of small steps (≤ 2 semitones) vs larger leaps.
  - `direction_changes`: number of times the melodic contour changes up ↔ down.
  - `repeat_rate`: proportion of repeated notes.
  - `avg_interval_size`: mean absolute interval size.
  - `note_density`: notes per bar (or per piece if bar info is missing).
  - `rhythmic_diversity`: count of distinct rhythmic values (quarterLength variants).

- **Bar / time-signature metrics**
  - `_compute_beats_per_bar_match_rate()` compares the sum of durations in each measure against the expected total from the declared time signature.
  - `_compute_expected_bars()` infers how many bars the piece should have and compares it to the observed bar count.
  - Derived booleans:
    - `beats_ok`: beats per bar mostly consistent with time signature.
    - `bars_ok_musical`: bar count consistent (within ±1) with expected length.

- **Key / tonal metrics**
  - `_compute_detected_key()` runs music21’s key analysis and normalizes the key string.
  - `_compute_in_key_share_and_cadence()`:
    - Measures the time spent in the declared key (time-weighted in-key percentage).
    - Checks whether the piece ends on the tonic.
    - Returns `in_key_pct_time_weighted`, `ends_on_tonic`, and a boolean `in_key_ok` based on a threshold.
  - `_compute_tonal_stability()`:
    - Splits the piece into segments.
    - Runs local key analysis per segment.
    - Reports a `tonal_stability_score` (how often segments agree), `key_changes`, and `drift_detected`.

- **Contour metrics**
  - `_compute_contour_analysis()` reduces the pitch line to up/down/same symbols and counts:
    - Dominant contour patterns,
    - `contour_diversity`,
    - `ascending_tendency` / `descending_tendency`,
    - `arch_shapes` and `downward_arches`.

- **Public API**
  - `eval_midi(midi_path, declared_key_pc_mode, declared_time, ...)`:
    - Wraps the call with timing and error handling.
    - Returns:
      - `ok` flag,
      - `seconds` (runtime),
      - `metrics` dict (all numbers above),
      - `error` string and `tooling` (music21 version).

This block gives us a **high-level musical profile** of each generation, which we later aggregate per batch (compile rates, scalar vs balanced style, stability, etc.).

---MARKDOWN CELL 5---
## Block 3 — LilyPond text-level evaluation (`eval_lily`)

This block inspects the **LilyPond source text itself** to see how well the model followed the prompt constraints, even before (or in addition to) actual rendering.

Key responsibilities:

- **Declared key/time & notation detection**
  - `_extract_declared()` parses the `.ly` file for:
    - Declared key as `(tonic_pc, mode)`
    - Declared time signature `(num, den)`
    - Notation type: `"relative"` vs `"absolute"` (based on `\relative` usage)

- **Body extraction & tokenization**
  - `strip_comments()` removes `%` line comments and `%{ ... %}` block comments.
  - `extract_music_body()` tries to isolate the main music block:
    - Prefer a `\relative c' { ... }` block.
    - Otherwise take the first `{ ... }` block or fall back to whole cleaned text.
  - `tokenize_notes_and_bars()` turns the body into a flat sequence:
    - `("note", name, accidental, marks, duration, dots)`
    - `("bar",)` and `("bar_cmd", ...)`
  - `bars_from_tokens()` groups tokens into literal bars.

- **Prompt adherence checks**
  - Notation policy:
    - If `expected_notation="relative"`, require at least one `\relative c' { ... }` anchor.
    - If `expected_notation="absolute"`, require no `\relative`.
  - Case policy:
    - When `require_lowercase=True`, disallow uppercase note names (C, D, E, …).
  - Forbidden constructs:
    - Rests (`r`), chords (`<...>`), multi-voice constructs (`<< >> \\`), repeats, tuplets, ties, grace notes, skips, `\score` and `\layout` blocks inside the body, etc.
  - Structural checks:
    - `bar_count_literal`: number of bars inferred from `|` / `\bar`.
    - `final_barline_literal_ok`: last token is a bar/bar_cmd (optional).
    - `bars_literal_ok`: bar count close (±1) to `expected_bars_literal`.
    - `notes_per_bar_literal_ok`: bar durations close to `expected_notes_per_bar_literal` in quarter-note units.
  - Accidentals policy:
    - Optionally disallow accidentals if `allow_accidentals=False`.

- **Compile / parse validation**
  - If `lilypond_bin` is provided:
    - Write a temp `.ly` (inserting `\version` if missing).
    - Invoke LilyPond with a “null” backend to just check for syntax.
    - Capture return code and a truncated error message.
  - Otherwise, or if LilyPond is not available:
    - Try a music21-based LilyPond parse as a lighter sanity check.
  - Fields:
    - `compiles` (True/False/None),
    - `compile_error`,
    - `compile_via` (`"lilypond"` / `"music21"` / None),
    - `parser_error` (for music21 failures).

- **Adherence score**
  - Collect all boolean flags (key/time present, notation_ok, lowercase_ok, no_forbidden, etc.).
  - Compute a normalized `adherence_text` in `[0,1]` plus a list of `failed_flags`.

- **Public entry**
  - `eval_lily(out_ly, ...)`:
    - Loads a `.ly` file from disk.
    - Calls `evaluate_lilypond_text()` with our configuration.
    - Wraps the result with timing and `tooling` info (`lilypond_bin` path).

This block tells us **how “clean” and on-spec the LilyPond text is**, independently of whether the MIDI rendering and musical content are good.

---MARKDOWN CELL 7---
## Block 4 — Integrated pipeline runner & batch summaries

This final block stitches everything together into a **single end-to-end pipeline** operating on the `prompt_lab_outputs` directory.

Main responsibilities:

- **Paths & environment**
  - `_nb_base_dir()` and `OUTPUTS_ROOT` define where to look for:
    - `raw/` → model generations (`out_*.ly`)
    - `midi/` → rendered MIDI and logs
    - `eval_midi/` → MIDI evaluation JSONL
    - `eval_lily/` → text evaluation JSONL
    - `final/` → combined per-run + per-batch summaries
  - `find_lilypond_bin()` tries to auto-detect a LilyPond binary and reports it in a small Markdown banner.

- **Batch discovery & bookkeeping**
  - `_collect_batches()` groups `out_*.ly` files by batch subdirectory.
  - `list_batches()` lists available batch names.
  - `_load_generation_map()` loads `summary.json` from generation-time and maps `run` indices to generation metadata (tokens, elapsed seconds, model, etc.).

- **Empty-output detection**
  - `_is_effectively_empty_ly()` and helpers detect “empty” generations:
    - Only whitespace/comments.
    - No notes, blocks, or key/time directives.
  - These are flagged upfront as `status="empty"` and skip heavy stages.

- **Status logic**
  - `_status_from(ok_midi_render, ok_midi_eval, ok_lily_eval, is_empty, compiles)` collapses all stage outcomes into a single label:
    - `"empty"`
    - `"failed_to_compile"` (LilyPond text doesn’t compile/parse)
    - `"ok"` (render, MIDI eval, and Lily eval all ok)
    - `"failed_to_convert_to_midi"` (Lily eval ok but MIDI path failed)
    - `"failed"` (other combinations)

- **Per-file pipeline**
  For each `out_*.ly` in a batch:
  1. Detect `is_empty` and try to extract declared key/time/notation with `_extract_declared()`.
  2. Run **Lily → MIDI** via `lily_to_midi()` (unless empty).
  3. Run **MIDI evaluation** via `eval_midi()` if MIDI exists.
  4. Run **Lily text evaluation** via `eval_lily()` (unless empty).
  5. Consolidate everything into a single `final_rec` with:
     - generation metadata,
     - render + MIDI metrics,
     - Lily metrics,
     - `status` and a `style_label` (`"scalar"` vs `"balanced"` based on step/entropy heuristics).
  6. Append that record to `final/index.jsonl`.

- **Human-readable tables**
  - Build a per-batch DataFrame with key columns (compile status, musical metrics, adherence, style label, note counts, etc.).
  - Convert booleans into emojis (✅/❌) where appropriate.
  - Round numeric values for display (3 decimals).
  - Special formatting:
    - `in_key_pct_time_weighted` and `adherence_text` are shown as percentages if they look like fractions.

- **AVG row & JSON summary**
  - Compute an extra **AVG row** per batch:
    - Percent “ok”.
    - Compile / in-key / beats / bars / drift rates.
    - Mean in-key percentage, beats-per-bar match rate, tonal stability, contour diversity, ascending tendency.
    - Mean adherence, plus “% scalar” among compiled rows.
    - Average note count and literal bar count over successful runs.
  - Attach this AVG row to the bottom of the displayed table.
  - Serialize it to `final/AVG_row.json` for downstream analysis (plots, thesis tables).

- **Public API & simple UI**
  - `run_full_pipeline_for_all(...)` is the main programmatic entry point; it returns:
    - `dfs_by_batch`: the displayed DataFrames (including AVG row)
    - `avg_rows_by_batch`: plain dict summaries.

This block turns the three previously defined components—renderer, MIDI evaluator, and Lily text evaluator—into a **repeatable, dataset-wide evaluation pipeline** with structured logs and ready-to-use aggregate metrics.

---MARKDOWN CELL 9---
--------------------------
---MARKDOWN CELL 10---
<div style="background-color:#f2f2f2;padding:10px;border-radius:8px;">
  <h3 style="color:black;">🎵 Relative vs Absolute</h3>
</div>
---MARKDOWN CELL 11---
## 🧪 Relative
---MARKDOWN CELL 13---
--------------
---MARKDOWN CELL 14---
## 🧪 Absolute
---MARKDOWN CELL 16---
### Observation
Across all 20 runs, both relative and absolute notation reach 100% compile success and perfect structural metrics, but their musical behavior diverges sharply. Relative notation produces extremely uniform melodies, with interval entropy fixed at 0.869, avg interval size at 1.71, step_share and step_vs_leap both at 1.0, and zero direction changes, resulting in consistently scalar, repetitive motion. Absolute notation, by contrast, generates far richer contour and pitch activity, with interval entropy rising to 1.696, avg interval size to 2.416, and direction changes averaging 5.7, while maintaining 100% in-key accuracy. This expressive gain comes at a cost: absolute outputs average 1860 tokens per run versus 1121 in relative, increasing verbosity and generation time (7.28s vs. 6.97s). In short, relative notation yields stable but flat patterns, while absolute notation produces more varied, musically alive phrases.
---MARKDOWN CELL 17---
----------
---MARKDOWN CELL 18---
<div style="background-color:#f2f2f2;padding:10px;border-radius:8px;">
  <h3 style="color:black;">🎵 Single-container vs Multi-container</h3>
</div>
---MARKDOWN CELL 19---
## 🧪 Multi-container
---MARKDOWN CELL 21---
### Observation
For the single-container baseline, we simply reuse the relative-notation results obtained earlier, since that setup already represents the single-container format without needing to regenerate anything. Compared to those relative outputs, which are extremely stable with 100% compile success, interval entropy fixed at 0.869, avg interval size at 1.71, zero direction changes, and a lightweight footprint of 1121 tokens and 6.97s per run, the multi-container format behaves very differently. Multi-container compile success drops to 90%, token count nearly doubles to 2230, and generation time more than doubles to 14.24s. Musically it becomes far more volatile, with interval entropy rising to 1.564, avg interval size to 3.47, and direction changes jumping to 4.33, alongside a clear increase in tonal drift (tonal stability = 0.362 vs 0.000 in single-container). Overall, the reused single-container (relative) outputs are compact, predictable, and stable, while multi-container generation is heavier, slower, and dramatically more erratic both structurally and musically.
---MARKDOWN CELL 22---
----------------
---MARKDOWN CELL 23---
<div style="background-color:#f2f2f2;padding:10px;border-radius:8px;">
  <h3 style="color:black;">🎵 Inline variables/macros ON vs OFF</h3>
</div>
---MARKDOWN CELL 24---
## 🧪 Inline variables/macros OFF
---MARKDOWN CELL 26---
### Observation
For the inline-variables/macros ON condition, we simply reuse the stable relative-notation results obtained earlier, since that setup already represents the model generating music with inline macros enabled. Those runs reach 100% compile success, remain extremely uniform with interval entropy fixed at 0.869, avg interval size at 1.71, zero direction changes, and stay lightweight at 1121 tokens with an average generation time of 6.97s. Turning inline variables/macros OFF completely breaks this stability: compile success collapses to 5%, generation becomes extremely heavy with an average of 3419 tokens and 24.8s per run, and almost every output fails to compile. Even the few successful cases show chaotic musical behavior, including interval entropy jumping to 1.482, avg interval size to 2.419, and 14 direction changes, with tonal stability dropping to 0.05. In short, leaving inline variables ON (as in the reused relative baseline) yields compact, predictable, fully valid sequences, while turning them OFF produces extremely verbose, unstable, and almost entirely non-compiling outputs.
---MARKDOWN CELL 27---
-----------
---MARKDOWN CELL 28---
<div style="background-color:#f2f2f2;padding:10px;border-radius:8px;">
  <h3 style="color:black;">🎵 Expanded repeats ON vs OFF</h3>
</div>
---MARKDOWN CELL 29---
## 🧪 Expanded repeats OFF
---MARKDOWN CELL 31---
### Observation
For the expanded repeats ON condition, we simply reuse the stable relative-notation results obtained earlier instead of regenerating new samples, since that setup already corresponds to fully expanded material: this gives us 100% compile success, perfectly clean structure, and very uniform melodies with interval entropy fixed at 0.869, avg interval size 1.71, zero direction changes, and compact sequences around 1121 tokens and 6.97s per run over 8 bars. When we turn expanded repeats OFF, the picture changes a lot: compile success drops to 90%, the model’s outputs roughly double in size to 2348 tokens with an average generation time of 15.8s, and bar structure shrinks to around 4 bars on average because the model starts relying on shorter notated patterns plus repeat constructs. Musically, the OFF setting becomes more volatile, with interval entropy increasing to 1.079, avg interval size to 2.31, and direction changes to about 2.39, along with more tonal drift (tonal stability = 0.212, drift_detected in 85% of runs) and lower textual adherence (71.5%). In short, reusing the expanded relative baseline gives us compact, fully explicit, highly predictable music, while keeping repeats unexpanded makes generations heavier, slower, structurally shorter on the page, and noticeably less stable both tonally and behaviorally.
---MARKDOWN CELL 32---
----------------
---MARKDOWN CELL 33---

<div style="background-color:#f2f2f2;padding:10px;border-radius:8px;">
  <h3 style="color:black;">🎵 Resolve or keep \transpose</h3>
</div>
---MARKDOWN CELL 34---
## 🧪 Keep \transpose
---MARKDOWN CELL 36---
### Observation
For the “resolve \transpose” condition, we simply reuse the stable relative-notation results obtained earlier instead of regenerating anything, since that setup already corresponds to all transpositions being resolved at the note level; this gives us 100% compile success, perfectly consistent structure, and very flat melodic behavior with interval entropy = 0.869, avg interval size = 1.71, 0 direction changes, full key adherence (100% in_key_ok, tonal_stability_score = 0.000), and compact outputs around 1121 tokens and 6.97s per run. When we keep \transpose in the prompt, the system becomes much less stable: compile success drops to 60%, average length explodes to 3876 tokens with 28.77s generation time, and even the compiled outputs show more turbulent pitch behavior, with interval entropy rising to 1.246, avg interval size to 2.91, and 8.58 direction changes on average. Tonal behavior degrades as well, with in_key_ok falling to 25%, time-weighted key adherence to 49.1%, and tonal_stability_score ≈ 0.013, while textual adherence also drops to 57.5%. In short, reusing the “resolved \transpose” (relative) baseline gives us short, clean, fully in-key melodies, whereas keeping \transpose in the input makes generations longer, slower, less reliable to compile, and much less stable tonally.
---MARKDOWN CELL 37---
-----------
---MARKDOWN CELL 38---
<div style="background-color:#f2f2f2;padding:10px;border-radius:8px;">
  <h3 style="color:black;">🎵 \tuplet vs \times</h3>
</div>
---MARKDOWN CELL 39---
## 🧪 Normalize to \tuplet a/b { … }
---MARKDOWN CELL 41---
------------------------
---MARKDOWN CELL 42---
## 🧪 Keep \times a/b { … }
---MARKDOWN CELL 44---
### Observation
When we normalize everything to \tuplet a/b { … }, the model behaves noticeably more sanely than when we keep \times a/b { … } in the prompt. With tuplets, we get 80% compile success, reasonably solid structure (80% bars_ok_musical, 75% beats_ok, beats_per_bar_match_rate = 0.787), and musically active but still controlled lines: interval_entropy = 1.476, avg_interval_size = 2.17, about 2.94 direction changes, and decent tonal behavior (75% in_key_ok, 79.8% time-weighted, tonal_stability_score = 0.270). Textual adherence is not amazing but workable at 48.5%, with outputs around 3613 tokens and 25.35s per run. Keeping raw \times a/b { … } is clearly worse on almost every axis: compile success drops to 50%, rhythmic correctness degrades (beats_ok = 40%, beats_per_bar_match_rate = 0.472), and tonal metrics fall off (in_key_ok = 45%, time-weighted 49.1%, tonal_stability_score = 0.160), while adherence plunges to 35% and sequences get even heavier (4025 tokens, 26.44s). In short, \tuplet-normalized prompts are still somewhat noisy but clearly more reliable, musical, and in-key than keeping raw \times constructs.
---MARKDOWN CELL 45---
-----------------
---MARKDOWN CELL 46---
<div style="background-color:#f2f2f2;padding:10px;border-radius:8px;">
  <h3 style="color:black;">🎵 Expand music functions ON vs OFF</h3>
</div>
---MARKDOWN CELL 47---
## 🧪 Expand music functions OFF
---MARKDOWN CELL 49---
### Observation
For the **“expand music functions ON”** condition, we just reuse the stable **relative-notation baseline** instead of regenerating anything, since that setup already corresponds to all music functions expanded explicitly into notes; this gives us **100% compile success**, clean structure, and very flat but fully in-key melodies, with **interval_entropy = 0.869**, **avg_interval_size = 1.71**, **0 direction changes**, **100% in_key_ok**, **tonal_stability_score = 0.000**, perfect adherence (**100%**), and compact outputs around **1121 tokens** and **6.97s** per run over **8 bars**. When we leave music functions **unexpanded (OFF)**, the system basically falls apart: compile success collapses to **20%**, structural checks almost vanish (**bars_ok_musical = 5%**, **beats_ok = 5%**, **beats_per_bar_match_rate = 0.144**), and the few “ok” runs are much heavier (**4320 tokens**, **24.0s** on average) and musically unstable, with **interval_entropy = 1.201**, **avg_interval_size = 2.59**, only **20% in_key_ok**, **tonal_stability_score = 0.037**, and extremely poor textual adherence (**13%**). In other words, expanding music functions into literal notes (the reused relative baseline) yields short, consistent, fully valid training targets, while keeping high-level music functions in the prompt leads to bloated, slow, mostly non-compiling, and structurally unreliable outputs.

---MARKDOWN CELL 50---
-----------------
---MARKDOWN CELL 51---
## Conclusion
Overall, these experiments make the pattern pretty clear: whenever the model is given a cleaner, more explicit, more “flattened” musical representation, it behaves predictably and produces compact, fully valid scores. Every setting that keeps the notation simple—relative pitches, single-container structure, inline variables ON, expanded repeats ON, resolved transpositions, expanded music functions, and normalized tuplets—leads to the same outcome: 100% compile success, perfect rhythmic structure, stable key behavior, and very uniform melodic motion.

On the other hand, every setting that leaves symbolic shortcuts in place—multi-container prompts, no inline variables, unexpanded repeats, raw \transpose, raw \times, or unexpanded music functions—introduces a ton of instability. These variants consistently produce longer sequences, slower generations, lower adherence, more compile failures, and very uneven tonal or structural behavior. In several cases (e.g., disabling inline variables or keeping music functions unexpanded), the model almost collapses entirely, with 5–20% compile success and extremely noisy musical output.

Taken together, the picture is simple:

- Explicit, normalized notation gives the model a stable surface to work on.

- Symbolic shortcuts push it outside its comfort zone and lead to longer, noisier, less reliable generations.

This gives us a strong justification for the normalization pipeline we’re building: the more we flatten and standardize the musical representation before fine-tuning, the cleaner and more controllable the model's behavior becomes.

